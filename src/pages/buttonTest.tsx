import Button from '@components/Button/Button';
import React from 'react';

const ButtonTest = () => {
  return (
    <div>
      <Button type="contained" text="loading" isLoading />
    </div>
  );
};

export default ButtonTest;
